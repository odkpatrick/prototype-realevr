import * as React from "react";
import Layout from "../components/layout";

const AccountPage = () => {
  return (
    <Layout>
      <h1>Account</h1>
    </Layout>
  );
};

export default AccountPage;

export const Head = () => <title>Realevr Tours || Account </title>;
