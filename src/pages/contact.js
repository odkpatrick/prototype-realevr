import * as React from "react";
import Layout from "../components/layout";

const ContactPage = () => {
  return (
    <Layout>
      <h1>Contact Us</h1>
    </Layout>
  );
};

export default ContactPage;

export const Head = () => <title>Realevr Tours || Contact Us</title>;
